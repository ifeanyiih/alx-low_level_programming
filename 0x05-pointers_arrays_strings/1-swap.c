#ifndef MAIN_H
	#define MAIN_H "main.h"
#endif
#include MAIN_H

/**
* swap_int - swaps the values of two integers
* @a: integer argument
* @b: integer argument
*/
void swap_int(int *a, int *b)
{
	temp = *a;
	*a = *b;
	*b = temp;
}
